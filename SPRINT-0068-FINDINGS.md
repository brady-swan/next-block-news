# Plan 0068 — reporting execution and useful visuals

September 8, 2026. Owner-approved; independent plan and implementation review approved.
Release verification is recorded below. Nano Banana remains parked and autopost remains OFF.

## What changed

- One precise event-identity correction shares the existing receipt repair allowance, inside
  the same six responses/360 seconds. Preview is side-effect-free and valid sibling stories
  survive. Later failures appear in eligible candidate retry context without guessed notebooks.
- Text-only editor requests no longer ask for image fields. Received malformed JSON or invalid
  references receive the existing one bounded recovery, with specific errors. Visual requests
  require explicit image decisions, not prose approval. No new transport/refusal retry loop.
- The editor can choose an exact supplied reader receipt. Source selection stays consistent
  through first reply, provenance, memory, Desk and later confirmed visual changes. Publisher
  materialization uses small evidence pointers rather than duplicating full articles.
- Related-outcome preparation, meaningful Bitcoin selection and dated reporting guidance were
  refined as approved. Relevant image pointers, ranking and two valid renderer examples make
  existing tools easier to use. Guidance explains at-a-glance understanding and potential reach.
- The Desk labels editor failures as human-review fallback and shows the reader-facing source.

No new model, worker, cadence, quota, budget, mandatory chart or primary-only rule. No existing
Typefully draft was manually changed, published or dismissed. Owner comments/edits stay intact.

## Independent review

The existing independent lead reviewed the plan, then implementation. Concrete findings fixed:
side-effect-free identity preview; preserved siblings; exact clipped evidence fingerprints;
confirmed-only source continuity after visual edits; bounded publisher payloads; and specific
per-story contract errors. The lead independently ran 105 focused tests before approval.

The paid image replay then found that optional visual fields could be omitted even when the
model approved the chart in prose. The lead approved requiring the four visual fields only for
requests containing visuals. An actual xAI adapter regression covers mixed batches and text-only
recovery: unused visual fields and requirements disappear again. Runtime validation is unchanged.

## Isolated model replays

Existing Grok 4.3 medium writer / Grok 4.5 medium editor, frozen retained evidence, no native
search, independent local SQLite/assets and no publisher/Typefully calls. One persistent $2
reservation ledger across both invocations. **12 calls; xAI-reported total $0.2954373.**
No Gemini key use. The second invocation replayed only the saved visual-editor input.

| Case | Observed result | What it establishes / does not |
| --- | --- | --- |
| Treasury malformed editor case | Valid initial drop: routine dated schedule | Contract recovered, not proof Brady's recent stepped-up program thesis is unnewsworthy |
| China gold | Valid initial publish; concise three paragraphs | This copy survived; no new reader source selected |
| AUSTRAC | Valid initial revise | Still passed an owner-rejected selection; editorial tuning is not solved |
| Metaplanet | Recovered decision on second call | Recovery worked, but copy remained too detailed |
| CLARITY | Writer removed broad storyline from exact-event field; valid distinct event | Code-derived correction preserved useful Semafor evidence; not a full production delivery test |
| Canaan deliberate chart exercise | Writer rendered two charts, inspected them and selected the year-over-year comparison | Existing renderer/model path works when deliberately requested; not organic adoption |
| Canaan visual editor after contract fix | Exact image/hash explicitly approved on first call | Genuine structured pixel review; no upload or fresh-news publication claimed |

The chart compares Q2 2025 revenue of $100.2M with Q2 2026 revenue of $31.9M using the retained
company release. Pixels were visually inspected: readable labels, common zero baseline, correct
units/periods, source credit and NBN branding. See [diagnostic chart](output/replay-0068/canaan-revenue.png).
The copy still included unnecessary ETH/treasury context, and its private reader-value note said
“halved” rather than describing the roughly 68% decline. Those remain craft observations, not
evidence that the actual plotted figures were wrong. This chart is a historical test, not news.

The first replay's final report query used a nonexistent asset `path` column after model work
had completed. No paid work was lost: dossiers, editor returns, assets and charges were retained
in the isolated DB. The reporting query was fixed; only the required visual-editor replay ran
again. The script now also supplies the full bounded diagnostic receipt explicitly, avoiding an
unnecessarily clipped initial test packet. Production fetch/research tools were not disabled.

Limits: frozen cases omit the full recent feed and native live research, so they are not a
novelty or discovery-speed benchmark. The original-source selection path has deterministic
coverage; natural editor take-up remains to be measured. No first natural image-bearing draft
has been demonstrated by these tests. No new Typefully transport test was needed; Plan 0066
already verified the unchanged image upload, alt text and first-reply delivery path.

## Verification and release

- Full working-tree Python suite: **577 passing tests**, including 11 new reporting-execution tests.
- Desk type check/build and browser-independent polling tests passed.
- Two unrelated owner evaluator tests/changes are excluded from the clean release.
- Clean-release test results, backup, deployment, live smoke and audit resumption: pending below.

## What the resumed audit must measure

Fewer avoidable identity holds, accurate retry objectives, initial/recovery/fallback editor rates,
actual reader-source choice and delivery consistency, later-outcome misses, story selection and
concise writing. Separate natural visual discovery, rendering, pixel review and confirmed delivery;
measure extra cost/latency and whether the image adds understanding. No illustration quota.

Keep the full editorial checkpoint **2026-09-08T11:15:56.744Z** and backfill the build interval.
Health/replay checks do not advance it. Existing autonomy and quiet-notification scope are retained.
Nano Banana is parked until Brady explicitly reopens it; using existing charts is not permission.

Rollback is code-only to the pre-sprint release (`0e8cc79`, runtime `0232620`), not destructive
database restoration. Preserve posts, additive tables, visual assets and pending delivery intents.
