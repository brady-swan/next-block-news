# Overnight improvement ledger — September 14–15, 2026

## Baseline / scope

Owner requests active, reversible improvement, not merely observation. Fixed cutoff13:00UTC.
Repo HEAD before work:4c8b54c0ee09566dd6f135b040bdb5e88245e939. Existing unrelated dirty work
is preserved. Reporter prompts were not yet tracked, so exact copies of the live orientation,
calibration examples and workspace AGENTS.md are retained in
audit/archive/reporter-before-overnight-20260915/. A repo HEAD alone would NOT restore those.
Runtime supervisor already contains the tested cutoff fix from the prior session; it is not
being rebuilt. No production app code/configuration or existing Typefully content changed.

## Change01 — reporter-execution-2026-09-15-v1

Evidence: final three-hour report, especially FCA dense excerpt, EFF uninspected source
visuals, Lummis redundant caveats/speaker error, and Atkins index/access split. Owner specifically
requests not missing useful image opportunities. Existing owner calibration policy unchanged.

- Bring visual consideration into story selection/research. Name existing inspect/render paths,
  one clear takeaway, comparable sourced numbers, phone legibility, attribution and actual pixels.
- Prefer readable original chart/comparison or short excerpt over a dense legal screenshot.
  No image quota, invented series, reuse-permission bypass, Nano Banana or duplicate image draft.
- Named institution's direct indexes; separate quote-post speakers; zero results is not outage.
- Plain attribution, retaining meaning-changing qualifications and omitting generic audit prose.
- New finite-shift AGENTS instructions; later messages/versioned prompt updates explicitly read.

Validation passed: owner example bytes and verification/selection policy spans unchanged;
focused instruction invariants; installed prompt/source copies match; all10sandbox canaries
passed after AGENTS binding change. Supervisor effective model/profile checks passed, active
lease and13:00UTC cutoff verified. Message1300 acknowledged1303 at02:54:16UTC, exactv1.
First actual nbn_visual call recorded1314/1315; pixels/outcome not yet audited. Acknowledgment
proves delivery, not quality improvement. New overnight heartbeat ACTIVE in MAIN every15minutes.
Roll back these prompt files from the exact baseline, install matching runtime copies and
recertify sandbox before any controlled same-shift process resume; keep13:00UTC cutoff.
Do not reset the whole repo or restore the database. Published/remote side effects are not
undone by git, so autopost and existing-draft protections remain in force.

## Next prioritized work during audit

1. Observe actual visual opportunity→inspection→render→pixel review→delivery; diagnose specific
   failures and fix obvious execution/tool usability gaps rather than just repeating advice.
2. Establish practical legitimate captured-evidence handoff for a source an operator can read
   but the reporter cannot; maintain capture attribution/limitations, not fabricated read claims.
3. Correct confirmed search empty-result classification if current code reproduces the defect.
4. Tune subsequent writing using owner goals and before/after evidence, one coherent delta at a time.

These are current authorized improvement priorities, not claims that code repairs are finished.
Independent review and proportional tests apply to substantive implementation. Record completed
changes here; don't resurrect old plans or treat every uncertain editorial preference as policy.
